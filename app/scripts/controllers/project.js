angular.module('maximmcnairApp')
  .controller('ProjectCtrl', function ($scope, $window) {
    // var windowEl = angular.element($window);
    // console.log
    // if($window.width > 1250){
    //   var windowPosition = windowEl.scrollTop();

    //   function parallax(topHeight, windowPosition, inertia){
    //     return topHeight - windowPosition / inertia;
    //   }

    //   angular.element(img).css(
    //     'transform', 'scale(1) translate3d(0, ' + parallax(0, windowPosition, 8) + 'px, 0)'
    //   )
    // } else {
    //   angular.element(img).css(
    //     'transform', 'translate3d(0, 0, 0)'
    //   )
    // }
  })